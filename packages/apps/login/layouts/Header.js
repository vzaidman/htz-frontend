import React, { Fragment, } from 'react';
import Head from 'next/head';

const Header = () => (
  <Fragment>
    <Head>
      <title>Login</title>
    </Head>
    <div>WELCOME</div>
    <br />
  </Fragment>
);

export default Header;
