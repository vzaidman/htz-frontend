import { createRenderer, } from '@haaretz/htz-components';

throw new Error(
  `You are currently using the default styleRenderer.
Please consider tweaking it in "StyleRenderer.js"
and make sure to remove this error`
);

const styleRenderer = createRenderer({ isRtl: true, });

export default styleRenderer;
