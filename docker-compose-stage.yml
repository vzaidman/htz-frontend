version: "3.3"
services:
  htz_react:
    build: .
    image: "docker.themarker.com:8083/htz_promotions:${version:-latest}"
    ports:
      - 2002:2002
    environment:
      - NODE_ENV=production
      # the following environment is optional
      - PORT=2002
      - BIND_ADDRESS=172.21.1.231
      - HOSTNAME=www
